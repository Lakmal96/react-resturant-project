import React from "react";
import Logo from "./Logo";

const Toolbar = (props) => {
  return (
    <div className="toolbar">
      <a>Menu</a>
      <Logo />
      <nav>...</nav>
    </div>
  );
};

export default Toolbar;
